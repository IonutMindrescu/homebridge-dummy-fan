{
  "pluginAlias": "DummyFan",
  "pluginType": "accessory",
  "singular": false,
  "schema": {
    "title": "Dummy Fan",
    "type": "object",
    "properties": {
      "name": {
        "title": "Name",
        "type": "string",
        "required": true,
        "default": "Dummy Fan",
        "description": "Name of Fan to appear in Home app"
      },
      "min": {
        "title": "Minimum Speed",
        "type": "integer",
        "default": 0,
        "description": "Minimum speed of the fan control (0 by default)"
      },
      "max": {
        "title": "Maximum Speed",
        "type": "integer",
        "default": 100,
        "description": "Maximum speed of the fan control (100 by default)"
      },
      "minStep": {
        "title": "Minimum Step",
        "type": "integer",
        "default": 1,
        "description": "The minimum step value (1 by default)"
      }
    }
  }
}
