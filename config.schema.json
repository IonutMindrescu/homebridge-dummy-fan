{
  "pluginAlias": "DummyFan",
  "pluginType": "accessory",
  "singular": false,
  "schema": {
    "title": "Dummy Fan",
    "type": "object",
    "properties": {
      "name": {
        "title": "Name",
        "type": "string",
        "required": true,
        "default": "Dummy Fan",
        "description": "Name of Fan to appear in Home app"
      },
      "autoOffDelay": {
        "title": "Auto Turn Off Delay",
        "type": "integer",
        "default": 0,
        "description": "Number of seconds after fan will automatically turn off (0 to disable)"
      }
    }
  }
}